{
    "id": "qwencode",
    "displayName": "Qwen Code (CLI)",
    "homepage": "https://qwen.ai/qwencode",
    "docs": "https://qwenlm.github.io/qwen-code-docs/en/users/overview/",
    "github": "https://github.com/QwenLM/qwen-code",
    "kind": "cli",
    "platforms": ["windows", "macos", "linux"],
    "install": {
        "curl (macOS / Linux — official standalone installer)": "curl -fsSL https://qwen-code-assets.oss-cn-hangzhou.aliyuncs.com/installation/install-qwen-standalone.sh | bash",
        "powershell (Windows — official standalone installer)": "irm https://qwen-code-assets.oss-cn-hangzhou.aliyuncs.com/installation/install-qwen-standalone.ps1 | iex",
        "npm (all platforms — alternative)": "npm install -g @qwen-code/qwen-code@latest",
        "WARNING": "The npm package name is EXACTLY '@qwen-code/qwen-code'. Do NOT install 'qwen-code' or 'qwen' alone (wrong packages). The CLI binary that lands on PATH is `qwen` (not `qwen-code`).",
        "note": "Prefer the official standalone installer for the target platform. The standalone archive does not require a separate Node installation; if the Unix script falls back to npm because the archive is unavailable, that branch requires Node.js 22+ and npm. Manual npm installation has the same requirement. After install, refresh PATH and run `qwen --version`. Config remains ~/.qwen/settings.json."
    },
    "install_flow": {
        "summary": "Official standalone installer or Node 22+ with npm → refresh PATH → verify",
        "agent_steps": [
            "Detect OS and architecture, then run the matching official standalone installer. Only check/provision Node.js 22+ and npm when using the npm alternative or its fallback branch.",
            "npm alternative: run `npm install -g @qwen-code/qwen-code@latest`. On Windows, the standalone installer creates %LOCALAPPDATA%\\qwen-code\\bin\\qwen.cmd; use that full path if the current shell has stale PATH.",
            "After install, run `qwen --version` to confirm. The CLI binary is `qwen`, not `qwen-code` — point this out to avoid confusion.",
            "Tell the user that model selection happens via Echobird's App Manager — clicking 'Update model config' writes ~/.qwen/settings.json with the chosen base URL / API key / model."
        ],
        "tell_user": [
            "Node.js prerequisite (and how to install if missing)",
            "That the CLI binary is `qwen`, not `qwen-code`",
            "That ~/.qwen/settings.json is the config file Echobird writes when applying a model"
        ]
    },
    "network_requirements": {
        "test_urls": ["https://registry.npmjs.org/@qwen-code/qwen-code", "https://github.com/QwenLM/qwen-code"],
        "mirrors": {
            "npm (China)": "npm install -g @qwen-code/qwen-code@latest --registry=https://registry.npmmirror.com"
        }
    },
    "identity_guard": {
        "description": "Qwen Code is Alibaba/QwenLM's open-source coding agent CLI (`@qwen-code/qwen-code` on npm, QwenLM/qwen-code on GitHub). It is a Qwen-optimized fork of Google's Gemini CLI. It is NOT 'Qwen Coder' (the model itself) and NOT 'Qwen-Agent' (a different agent framework) — they are separate products that happen to share the Qwen brand."
    }
}
